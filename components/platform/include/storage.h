/**
 * storage.h — Platform Services: Non-Volatile Storage
 *
 * Platform services sit between game/presentation and hal. They abstract
 * hardware-specific concerns (NVS, LittleFS) behind clean interfaces.
 *
 * Architecture constraint: platform/ MAY depend on hal/. game/ and
 * presentation/ MUST NOT depend on platform/ directly — they receive
 * platform services via dependency injection from the application layer.
 *
 * Placeholder — expanded in later phases.
 */

#ifndef FIESTAQUEST_PLATFORM_STORAGE_H
#define FIESTAQUEST_PLATFORM_STORAGE_H

#include <stdint.h>

/* Placeholder: expanded in later phases. */

#endif /* FIESTAQUEST_PLATFORM_STORAGE_H */
