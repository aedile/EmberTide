/**
 * types.c — FiestaQuest Game Engine: Shared Value Types (placeholder)
 *
 * Placeholder translation unit so CMake has at least one .c file to compile
 * for the game component during the host test build. Expanded in later phases.
 */

#include "types.h"

/* No implementation required for value-only types in phase 1. */
