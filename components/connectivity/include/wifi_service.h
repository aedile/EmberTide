/**
 * wifi_service.h — Connectivity: WiFi Service
 *
 * Architecture constraint: connectivity/ MUST NOT include game/ headers.
 * It communicates with the application layer exclusively via typed event
 * structs passed through the event bus.
 *
 * Placeholder — expanded in later phases.
 */

#ifndef FIESTAQUEST_CONNECTIVITY_WIFI_SERVICE_H
#define FIESTAQUEST_CONNECTIVITY_WIFI_SERVICE_H

#include <stdint.h>

/* Placeholder: expanded in later phases. */

#endif /* FIESTAQUEST_CONNECTIVITY_WIFI_SERVICE_H */
